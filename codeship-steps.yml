- type: parallel
  name: example_parallel_step
  steps:
    - service: ubuntu1804
      command: /bin/bash -c 'apt-get update && apt-get install -y sudo && rm -rf /var/lib/apt/lists/* && ./environment_setup/setup_software.sh melodic'
